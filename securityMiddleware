const express = require("express");
const router = express.Router();
const validateSource = require("../middleware/securityMiddleware");

router.post("/data", validateSource, async (req, res) => {
  res.json({ ok: true });
});

module.exports = router;
